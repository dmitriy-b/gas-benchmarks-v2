---
# Role: benchmark_validator
# Purpose: Default variables for benchmark validation

# Minimum required versions
min_docker_version: "20.10"
min_docker_compose_version: "2.0"
min_python_version: "3.10"
min_jq_version: "1.6"

# Paths to validate
required_scripts:
  - "src/run.sh"
  - "src/setup_node.py"

# Required packages
python_packages:
  - jq
  - psycopg2-binary
